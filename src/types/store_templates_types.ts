export interface IStoreTemplateTypes {
    _id: string;
    name: string;
    template: any; // Define the type of the template based on your requirements
    createdAt: string;
    updatedAt: string;
    isActive: boolean;
    storeId: string;
}